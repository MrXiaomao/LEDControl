待完成工作：
1、弹出界面选择电压配置文件，json格式，需要对文件的电压进行简单判断，是否成对，是否在取值范围内。
2、采用循环对电压进行遍历。
3、将PDGAInit函数部分拆成两个函数，一部分是循环前调用一次，其他的是循环中调用。
4、增加接受串口数据的处理，写完指令反馈函数BackSend的反馈判断，也就是指令是否下发成功。
5、增加日志文件。

补充的条目：
1、某些可配置参数需要设置上下限，超过范围，给出提示；
2、某些可配置参数只能选择特定点，给出提示，例如发光宽度只能为10的整数倍；
3、界面显示用户感兴趣的信息，例如发了何种指令等，将指令翻译成可读；
4、添加详细日志，包括原始指令和翻译后可读的语句。
5、界面布局，日志框可以缩放。